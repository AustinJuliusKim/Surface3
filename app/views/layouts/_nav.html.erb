<nav>
<div>
	<% if logged_in? %>
	Logged in as: <%= link_to "#{current_user.username}", current_user  %> | <%= link_to 'Log Out', logout_path, method: :delete %> | 
	<% else %>
	  <%= link_to 'Log In', login_path %> |
	<% end %>
	 <%= link_to 'Home', users_path  %> |
</div>



</nav>