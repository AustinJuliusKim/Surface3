<div>
	<section>
		<%= form_for @user do |f| %>
			<div>
				<%= f.label :username  %>
				<%= f.text_field :username  %>
			</div>

			<div>
				<%= f.label :password_digest  %>
				<%= f.password_field :password_digest  %>
			</div>

			<div>
				<%= f.label :password_digest  %>
				<%= f.password_field :password_digest  %>
			</div>

			<div>
				<%= f.label :email  %>
				<%= f.text_field :email  %>
			</div>

			<div>
				<%= f.submit "Submit", class: "btn btn-submit"  %>
			</div>
			<div>
				<%= link_to "Delete", :user, method: :delete, data: {confirm: "are you sure?"} %> 
			</div>
		<% end %>

	</section>

	<section>
		<!-- Where Profile Show will be -->
	</section>
</div>