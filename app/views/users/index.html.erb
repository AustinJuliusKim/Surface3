<div>
	<section>
		<h1>Welcome!</h1>
	</section>
	<section>
		<% @users.each do |user|  %>
			<div class="col-md-4">
				<li class="well-sm">
					<strong> <%= user.username %> </strong>
					<%= link_to "Show", user_path(user) %>
				</li>
				
			</div>
		<% end %>
	</section>
</div>